{
  "name": "clear402",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "attack:all": "node scripts/run_attack_lab.ts",
    "lint": "pnpm -r lint",
    "test": "vitest run --config vitest.config.ts && pnpm --filter @clear402/runtime test",
    "build": "pnpm -r build",
    "test:e2e": "pnpm --filter @clear402/runtime test guard && pnpm run attack:all"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
