# Demo Video

Use `recording-script.md` to record or verify the final 3-5 minute demo video.

Status: recording script and short repo preview are ready. Upload the final 3-5 minute demo video directly through the hackathon portal or provide its hosted URL there.

Repository short preview MP4:

```text
submission/demo-video/clear402-demo-preview.mp4
```

Recommended final portal filename:

```text
clear402-demo-3to5min.mp4
```

Recommended recording flow:

1. Start the runtime, provider, and dashboard.
2. Open the dashboard at `http://127.0.0.1:3000`.
3. Follow the timestamps in `recording-script.md`.
4. Show the terminal result for `pnpm run attack:all`.
5. Show the evidence export files or exported JSON/Markdown in the dashboard.
6. End on the repo README or the final PPT title slide.

Do not claim mainnet or production readiness in the narration. Use the exact live/fallback/mock boundary language from `docs/limitations.md`.
